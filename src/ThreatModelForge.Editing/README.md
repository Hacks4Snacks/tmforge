# ThreatModelForge.Editing

Portable, UI-agnostic editing operations for threat model diagrams: move, rename, and
delete elements, with snapshot-based undo/redo and native model serialization. Built on
`ThreatModelForge.Core` and free of any UI framework, so it can be driven by a web
front end, a CLI, or tests.
