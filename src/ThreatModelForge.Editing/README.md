# ThreatModelForge.Editing

Portable, UI-agnostic editing operations for threat model diagrams: move, rename, and
delete elements, with snapshot-based undo/redo and lossless save. Built on
`ThreatModelForge` and free of any UI framework, so it can be driven by a web
front end, a CLI, or tests.
